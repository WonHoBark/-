Java를 기본으로 하는 ko-kr(확장 언어로 변환) 프로그래밍 언어
기본 ko-kr로 다른 언어까지 확장가능
keyword allieas
한글로 프로그래밍 하나 Java로 변환 가능하게끔 함
Java Library를 추가할수 있게끔 함
Java keyword를 한글로 입력 가능하게 하고
꼭 하나씩만 링크 되는 것이 아니라 한글 다수에 java keyword 하나 링크를 기본으로 하고
키워드 입력 시 추천 키워드로 사용 빈도 순위를 보여 줌

★#를 이용한 소스 코드 검색과 소스코드 온라인에 추가
닉네임아이디 비밀번호 로그인방식으로 한가지 태그에 아이디당 한가지의 소스만 등록
검색시 사용률순위별로 닉네임과 소스코드를 미리보기

#만약에
=> list(1.topcodeer(채택율55%) : 만약 () 라면 ;
list(2.it’s me(채택율45%) : if () else;

#만약에 a보다b가 크면 A가 더 크다를 출력하고 아니라면 B가 더작다를 출력한다
변환=>  만약 (a>b) 라면 출력("A가 더크다") 아니라면 출력(“B가 더크다");
java변환=> if (a>b) print("A가 더크다") else print(“B가 더크다”);

★자연어를 해석하여 코딩하게 해준다.
